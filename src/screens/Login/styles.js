import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    container: { flex: 1, backgroundColor: '#a35a16', justifyContent: 'center', alignItems: 'center', },
    freddo: {
        color: '#fff',
        fontFamily: 'MarckScript-Regular',
        fontSize: 50,
    },
    background_image: { opacity: 0.8, position: 'absolute', width: '100%', height: '100%' },
    content: { flex: 4, justifyContent: 'center', alignItems: 'center', width: '100%' }
});
export default styles;
