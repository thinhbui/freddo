import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    container: { flex: 1, backgroundColor: '#a35a16', justifyContent: 'center', alignItems: 'center', },
    freddo: { color: '#fff', fontFamily: 'MarckScript-Regular', fontSize: 25 },

});
export default styles;
